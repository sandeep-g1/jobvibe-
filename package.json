{
  "name": "shortlist-india",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Daily de-duplicated shortlist of India jobs with verified apply links and ATS match scoring.",
  "scripts": {
    "seed": "node --no-warnings src/seed.js",
    "run": "node --no-warnings src/run.js",
    "serve": "node --no-warnings src/serve.js",
    "stats": "node --no-warnings src/stats.js",
    "reset": "node --no-warnings src/reset.js",
    "db:check": "node --no-warnings src/tools/db-check.js",
    "expand": "node --no-warnings src/tools/expand-registry.js",
    "secrets:push": "node --no-warnings src/tools/push-secrets.js",
    "email:test": "node --no-warnings src/tools/email-test.js",
    "gh:token": "node --no-warnings src/tools/push-gh-token.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "libsodium-wrappers": "^0.7.15"
  }
}
